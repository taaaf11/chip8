# chip8

Implementing a few instructions out of many instructions in chip8 instruction set. It can create an IBM logo, by reading program stored in `ibmlogo.ch8` file. Just run the py file. 

### requirements
```
pygame==2.6.1
```